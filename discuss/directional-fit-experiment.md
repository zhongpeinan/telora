# #193：保留类型信息的失配实验

分支：`fix/193-directional-fit`，基于 `5f481180`。实验已落实为 RFC 0297 实现；
完整逐项验收与性能数据见 [验收记录](rfc-0297-verification.md)。

## 实验范围

将两种情况拆开：

- 不能形成类型的错误仍可将槽标为 Conflicted，例如结构递归、未解析引用等。
- 两个已存在结构类型不匹配时，记录失败约束及两侧槽、位置、诊断，但不因此抹掉两侧类型。

父结构在子结构比较没有产生新冲突后才合并，避免提前合并后丢失实际类型。这仍是在原图上操作，没有复制环境、逐模块隔离或失败后回滚。

结构失配记录仍落在共享 equal 路径，因此影响不止 Fit。契约保护另有明确边界：
`solve_contracts` 在加入值证据前检查骨架闭合，记录声明契约是否就绪，并遍历已就绪
契约的结构及 nominal 成员建立保护标记。后续合并代表槽时保留该标记；特殊错误和
继承的 Conflicted 也不能覆盖这些骨架。普通局部推导槽不因第一次获得结构而变成契约。

## 已验证的改善

独立语言资产位于 `tests/language/src/check/diag-shared-contract/` 和 `diag-contract-local/`，内部状态由 `type_resolve/tests/contracts.rs` 检查。

| 观察 | main | 实验分支 |
| --- | --- | --- |
| 错误调用后的共享 increment 签名 | Conflicted | 保留 Fn(Int) -> Int |
| 健康模块的 answer | Conflicted | 保留 Int |
| 连续传入 String 和 Bool 的两次错误调用 | 仅第一次报告 | 两次分别报告 |
| 输入字符串变量 | 可能沿共享约束污染 | 保留 String |
| 失败 MIR 的 seal | 拒绝 | 仍拒绝，清除诊断文本后也拒绝 |

新断言还验证了：交换导入顺序、同模块泛型错用与正常实例共存、合法未注解调用继续推导，以及函数体违反已声明返回类型仍然失败。

## 当前落地与验证

- RFC 0297 的所有顶层普通值契约检查已实现。MIR 保存 DeclarationContract 义务，
  seal 重新校验；签名缺失不能因为实现最终推导成 Known 而绕过。
- `decl/def` 配对、显式泛型、类型别名和重导出沿用声明身份。普通值 alias 只在
  初始化时实例化一次，后续引用不再隐式泛化；直接模板引用独立实例化。
- 失败约束保存使用 HIR、两侧槽和精确诊断索引；`@check` 通过使用节点关联失败，
  query 可同时报告已知类型和失败约束。批量静态诊断区分规范模块与 session 选择器。
- Fit 记录来源注解，延迟产生类型的语法节点保留其接收的期望契约。失败约束保存
  contracts HIR 身份，并输出 `type contract declared here` secondary；不用合并后的
  类型代表槽猜来源。跨模块、反向加载、泛型错用和错误函数体均有来源断言。
- 特殊错误边界新增独立语言资产：错误 TypeOf 见证、非法诊断参数、调用 arity 错误
  及失败表达式的消费。内部断言检查契约仍 Known、独立根因保留、清除诊断后仍拒绝 seal。
- 初始化事件保存 Wasm 正在执行的初始化根；CLI 分别输出规则模块、subject 来源和
  initialization 根身份。共享函数/健康导入者/失败值依赖的用例确认根因只报告一次，
  初始化结束后该身份不进入 Entry 诊断。Wasm 75 项、CLI 非语言 100 项及语言 424 项通过。
- 已完成主要 std、生成源码及测试资产迁移。阶段 15 的语言验收为 424/424，
  workspace library 为 318 项，CLI 非语言为 99 项；新增特殊边界后 core 为 148 项。
- 真实 lab-ontology 已迁移：三个模型 query 测试共 92 项，ontology 自身正常测试
  415 项及 6 项预期失败诊断验证通过。world-model 的真实 eval-with 生成预期 SQL/参数。

上述测试记录对应各阶段提交，不替代最终提交后的完整验收。尚未给出本轮 release
性能对比结论。

## 最终验收

文档和 examples 的顶层示例已补齐签名；独立 examples 工作区全库检查通过。
workspace 全量测试及语言 427 项已通过，新增契约内部断言和 LSP 资产复验通过。
同语料 release 对比已完成：纯类型/完整 check 时间比 main 增加 6.3%–8.6%，
具体均值、标准差和 RSS 见验收记录。这是语义与诊断改进，不宣称性能收益。

未增加 isolate 或失败后重跑；没有通过丢弃错误使 seal 放行。
