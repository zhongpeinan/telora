import "std/codec" as codec;

type Type = struct {v: Option(String)};

def decode = fn(value) {
    codec.decode((Type).type, value)
};

def encode = fn(value) {
    codec.encode((codec.Value).type, value)
};

export { Type, decode, encode };
