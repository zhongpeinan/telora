import "./user.json" { data };
import "std/codec" as codec;
import "std/blame" {BlameError};

type User = struct {
    name: String,
    age: Int,
    nickname: Option(String),
};

def result: Result(User, BlameError) = codec.decode(User.type, data);
export { result as output };
