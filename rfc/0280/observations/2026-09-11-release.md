# Release observation, 2026-09-11

Observation requested after the generic-function and diagnostic acceptance work.
This records current data only: no baseline comparison, performance conclusion
or optimization follows from this sample.

- Telora source: `4ee3312`, branch `feat/0280-arena-type-consumers`.
- Build: `cargo build --release -p telora`, successful before measurement.
- Ontology source: `1a871a0da47ec677bccf4922d4432ccc2e254d7e`, clean worktree.
- Host: Linux x86_64, KVM, Intel Xeon Gold 6266C @ 3 GHz, 4 logical CPUs.
- Build and test processes were finished before timing.
- Hyperfine 1.20.0, 2 warmups and 5 measured runs per command, sequentially.
- No cache clearing or CPU pinning. Build time is excluded.

Commands, from the Telora repository:

```sh
target/release/telora -C /home/h00629578/ws/lab-ws/lab-ontology/ontology check --only-types @test/query
target/release/telora -C /home/h00629578/ws/lab-ws/lab-ontology/ontology check @test/query
```

| Mode | Mean ± sample stddev | Median | Min–max | Exit codes |
| --- | --- | --- | --- | --- |
| only-types | 281.6 ± 5.2 ms | 280.6 ms | 274.3–287.1 ms | all 0 |
| ordinary check | 325.0 ± 16.2 ms | 320.2 ms | 306.8–350.8 ms | all 0 |

Raw wall-time samples, seconds:

- only-types: 0.2743285864, 0.2870529664, 0.2860251274, 0.2806498904, 0.2797045234.
- ordinary: 0.3202462444, 0.3200783074, 0.3508012684, 0.3067871624, 0.3269305064.

Separate `/usr/bin/time -v` observations, one run per mode before Hyperfine:

| Mode | Wall | User / system | Peak RSS | Internal catalog / static / execution |
| --- | --- | --- | --- | --- |
| only-types | 0.27 s | 0.25 / 0.02 s | 41,604 KiB (40.6 MiB) | 0.716 / 265.516 / 0 ms |
| ordinary | 0.31 s | 0.29 / 0.02 s | 44,644 KiB (43.6 MiB) | 0.718 / 272.640 / 30.250 ms |

These phase and RSS values belong to the independent single runs, not to the
Hyperfine means. RSS measures resident process memory, not retained heap size.
Both summaries report status ok, 0 Unknown, 0 Conflicted, 0 unproven bounds,
230 bound requirements and 8 property records.

The originally requested relative `-C ../lab-ws/lab-ontology/ontology` form
exited before checking with `workspace member ontology must be a directory
inside the workspace`. The absolute path above reaches the same directory and
succeeds. The path-handling issue remains open; no package change was made for
this observation. The early-exit runs are excluded from these timings.

Raw artifacts are adjacent: `2026-09-11-hyperfine.json`,
`2026-09-11-{types-only,check}.jsonl` and
`2026-09-11-{types-only,check}.time`.

Acceptance at this source revision: workspace library tests 393/393, all-target
checking and release build pass; language tests 372/403, with 31 check cases
still requiring review. This observation does not close final architecture or
performance acceptance.
