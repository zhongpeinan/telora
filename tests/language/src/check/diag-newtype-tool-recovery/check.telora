use crate::support as support;
use std::value::{ Value };

pub def check: Fn(Value) -> Value = fn(actual) {
    support::bool_value(support::failed_with(actual, "computed metadata cannot become a type")
        && !support::stream_contains_string_field(actual, "stdout", "message", "tool-stage"))
};
