use std::type_desc::{TypeDesc};
type Prop = struct {};
def deco: Fn(TypeDesc, Option(Prop)) -> Prop = fn(target, previous) {
    let value: Prop = {};
    value
};
@deco type Target = struct {};
pub use self::{Prop, Target};
