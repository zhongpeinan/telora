trait Display { display: Fn(Self) -> String };
type Endpoint = struct {host: String};
impl Display for Endpoint { display: fn(value) { value.host } };
impl Display for Endpoint { display: fn(value) { value.host } };
pub use self::{Display, Endpoint};
