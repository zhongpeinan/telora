use std::test as test;
pub def not_run: test::Test = test::should_ok(fn() { fail!("must not run during check") });
pub def not_read: test::Test = test::with_fixtures(["missing.json"], fn(value) { fail!("must not invoke factory", value) });
