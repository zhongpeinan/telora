use std::value::{Value};
use crate::support as support;
use crate::unknown_support::{unknown_at};

pub def check: Fn(Value) -> Value = fn(actual) {
    support::bool_value(unknown_at(actual, "language-tests/check/diag-enum-constructor-context/testee", 1))
};
