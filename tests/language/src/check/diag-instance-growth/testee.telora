def grow: for(T) Fn(T) -> Int = fn(value) { grow([value]) };
pub def entry: Fn(Int) -> Int = grow@[Int];
pub def independent: Int = 42;
pub def wrong: Int = "independent type error";
trait Missing { value: Fn(Self) -> Int };
pub def missing: Int = Missing::value(42);
