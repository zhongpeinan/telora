export def stop: Fn(Int) -> Never = fn(x) { fail!("never callable invoked", x) };
export def accept: for(T) Fn(Fn(Int) -> T) -> Int = fn(f) { 1 };
export def indirect: for(T) Fn(T) -> Int = fn(value) { accept@[T](stop) };
