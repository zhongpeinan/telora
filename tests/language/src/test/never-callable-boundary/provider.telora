pub def stop: Fn(Int) -> Never = fn(x) { fail!("never callable invoked", x) };
pub def accept: for(T) Fn(Fn(Int) -> T) -> Int = fn(f) { 1 };
pub def indirect: for(T) Fn(T) -> Int = fn(value) { accept@[T](stop) };
