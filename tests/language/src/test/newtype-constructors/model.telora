pub type External = struct(Int);
pub type Packet(T) = struct(T);
pub def create: Fn(Int) -> External = External;
pub def wrap: for(T) Fn(T) -> Packet(T) = Packet;
pub def metadata: TypeOf(External) = External.type;
