export type External = struct(Int);
export type Packet(T) = struct(T);
export def create: Fn(Int) -> External = External;
export def wrap: for(T) Fn(T) -> Packet(T) = Packet;
export def metadata: TypeOf(External) = External.type;
