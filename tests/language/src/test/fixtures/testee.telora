use std::test as test;

pub def cases: test::Test = test::with_fixtures(
    ["a.json", "bad.json", "b.yaml", "c.toml", "a.json"],
    fn(value) {
        test::should_fail_with(fn() { fail!("fixture subject", value) }, "fixture subject")
    },
);
