use std::test as test;
use crate::test_support::{ expect };
use self::provider::{ wrap };

pub def concrete_dependencies: test::Test = test::should_ok(fn() {
    expect(wrap(7) == "int" && wrap("text") == "text" && wrap(8) == "int")
});
