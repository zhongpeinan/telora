use std::value::{Value};
def declared: (Int, String) = do {
    decl identity: for(T) Fn(T) -> T;
    def identity = fn(value) {value};
    (identity(42),identity("done"))
};
pub def answer: Value = if declared == (42,"done") {Value::True} else {Value::False};
