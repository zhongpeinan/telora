use std::test as test;
use self::values as values;

pub def physical_newlines: test::Test = test::should_ok(values::physical_newlines);
pub def explicit_carriage_returns: test::Test = test::should_ok(values::explicit_carriage_returns);
pub def continuations: test::Test = test::should_ok(values::continuations);
