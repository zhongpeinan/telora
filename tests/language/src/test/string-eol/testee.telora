import "std/test" as test;
import "./values" as values;

export def physical_newlines: test.Test = test.should_ok(values.physical_newlines);
export def explicit_carriage_returns: test.Test = test.should_ok(values.explicit_carriage_returns);
export def continuations: test.Test = test.should_ok(values.continuations);
