use std::test as test;
use crate::test_support::{ expect };
use self::left as left;
use self::right as right;

pub def ordinary_candidates: test::Test = test::should_ok(fn() {
    expect(match 76 { value => value == 76 })
});
