use std::test as test;
use crate::test_support::{ expect };
use self::model::{ Ok, Err };

pub def member_candidates: test::Test = test::should_ok(fn() {
    expect(match Ok { Ok => True, Err => False })
});
