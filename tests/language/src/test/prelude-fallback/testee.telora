import "std/test" as test;
import "@src/test-support" { expect };
import "@src/test/prelude-fallback/model" *;

export def member_candidates = test.should_ok(fn() {
    expect(match Ok { Ok => True, Err => False })
});
