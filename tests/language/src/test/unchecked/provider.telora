pub type Box(T) = struct {value: T};
pub def candidate: Unchecked(Box(Int)) = {value: 7};
pub def read: for(T) Fn(Unchecked(Box(T))) -> T = fn(candidate) {
    candidate.value
};
pub def finish: for(T) Fn(Unchecked(Box(T))) -> Box(T) = fn(candidate) {
    candidate
};
