use std::test as test;
pub def missing: test::Test = test::with_fixtures(
    ["absent.json", "stdin+json://", "https://example.test/a.json", "/tmp/a.json", "../../../../escape.json"],
    fn(value) { test::should_fail(fn() { value }) },
);
