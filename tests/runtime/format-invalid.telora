import "std/fmt" as fmt;
export def answer: (Array(String), Array(String)) = fmt.prepare("{name");
def invalid: (Array(String), Array(String)) = fmt.prepare("{1name}");
def concat: fmt.Fmt = fmt.concat([], []);
