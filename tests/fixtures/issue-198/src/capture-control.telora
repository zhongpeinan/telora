use std::value::{ Value };
type Message = enum { Item(Int) };
use Message::{Item};
def make: Fn(Int) -> Message = fn(value) { Item(value) };
pub def answer: Value = Value::Int(match make(1) { Message::Item(x) => x });
