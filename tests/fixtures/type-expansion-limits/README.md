# 类型展开边界测试

模板由 core 的 convergence 测试读取并替换占位符，用于构造临界尺寸的源码，
不通过 `include_str!` 嵌入。测试检查深类型和 Tuple 宽度的资源诊断、位置和 seal 拒绝，
以及限制内的合法类型仍能闭合。具体数字是 #190 分支的初始试验值。

layout/instance 模板验证源模板和输入各自未触限，但代入以后触限的情况，确保保护
不仅检查源码中显式出现的类型，也覆盖后续布局和函数实例展开。
