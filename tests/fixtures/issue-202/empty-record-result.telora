type Diagnostic = struct { message: String };
type Normalized = struct { diagnostics: Array(Diagnostic) };
pub def normalize: Fn() -> Result(Normalized, String) = fn() {
    Ok({ diagnostics: [] })
};
pub def unrelated: Array(Int) = [];
