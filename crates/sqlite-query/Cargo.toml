[package]
name = "sqlite-query"
version.workspace = true
edition.workspace = true
license = "MIT"
description = "Read-only SQLite query native actor component"

[dependencies]
anyhow = "1"
rusqlite = { version = "0.37", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
