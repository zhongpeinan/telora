pub def complete: Int = 42;
pub def identity: for(T) Fn(T) -> T = fn(value) { value };
def private_inferred = identity("healthy");
pub def missing = 42;
pub def alias = complete;
decl paired: Fn(Int) -> Int;
def paired = fn(value) { value };
pub use self::{paired};
def local_inference: Fn() -> String = fn() {
    let local = identity("healthy");
    local
};
