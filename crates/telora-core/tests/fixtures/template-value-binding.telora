use crate::library::{foo};

pub def valid: Fn() -> Int = fn() {
    let foo = foo;
    foo(foo(42))
};

pub def invalid: Fn() -> (Int, String) = fn() {
    let foo = foo;
    (foo(42), foo("different instance forbidden"))
};
