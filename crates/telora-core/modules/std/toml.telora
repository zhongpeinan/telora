use std::value::{ Value };
use std::codec::{ BlameError };

native parse_raw: Fn(TypeOf(Value), String) -> Result(Value, BlameError);
def parse: Fn(String) -> Result(Value, BlameError) = fn(text) {
    parse_raw(Value.type, text)
};

pub use self::{ parse, BlameError };
