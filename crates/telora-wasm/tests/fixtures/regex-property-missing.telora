import "std/regex" as regex;
import "std/type-property" as property;
@regex.parse_by(regex.compile("(?P<y>a)"))
type Invalid = struct { x: Int };
export def answer: Option(regex.ParseBy) = property.get_type_prop(Invalid.type, regex.ParseBy.type);
