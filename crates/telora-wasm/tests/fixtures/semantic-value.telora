use std::value::{Value};

pub def answer: Value = Value::Object({
    number: Value::Int(42),
    nested: Value::Array([Value::None, Value::True, Value::String("hello")]),
});

pub def identity: Fn(Value) -> Value = fn(value) { value };
