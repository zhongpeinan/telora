use std::regex as regex;
use std::type_property as property;
@regex::parse_by(regex::compile("(?P<x>a)?"))
type Invalid = struct { x: Int };
pub def answer: Option(regex::ParseBy) = property::get_type_prop(Invalid.type, regex::ParseBy.type);
