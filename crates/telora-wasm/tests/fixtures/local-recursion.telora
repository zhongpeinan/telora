export def answer = do {
    let base = 40;
    def even = fn(n: Int) -> Int {
        if n == 0 { base + 2 } else { odd(n - 1) }
    };
    def odd = fn(n: Int) -> Int {
        if n == 0 { 0 } else { even(n - 1) }
    };
    even(10)
};
