pub type LocalizedText = Dict(String);
@property(PropertyTarget::Type)
type Decorated = struct { display: LocalizedText };

pub def decorate: Fn(LocalizedText, LocalizedText) -> Fn(Type, Option(Decorated)) -> Decorated =
    fn(display, description) { fn(target, previous) { {display: display} } };
